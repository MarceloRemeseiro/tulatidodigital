---
---

<section id="contacto" class="py-20 md:py-32 bg-background section-fade-in">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center">
      <h2 class="text-3xl md:text-4xl font-extrabold text-foreground">
        Tu Negocio Merece Respirar Digital. ¡Conectemos!
      </h2>
      <p class="mt-4 text-lg text-foreground/80">
        Escríbenos y cuéntanos dónde estás. Nosotras te decimos cómo hacer que tu marca vuelva a latir con fuerza.
      </p>
    </div>
    <div class="mt-12 bg-background-secondary p-8 rounded-2xl shadow-2xl border border-foreground/10">
      <form action="#" method="POST">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <label for="nombre" class="block text-sm font-medium text-foreground/90">Nombre</label>
            <input type="text" name="nombre" id="nombre" class="mt-1 block w-full px-4 py-3 bg-background border border-foreground/20 rounded-lg shadow-sm focus:ring-primary focus:border-primary text-foreground">
          </div>
          <div>
            <label for="email" class="block text-sm font-medium text-foreground/90">Email</label>
            <input type="email" name="email" id="email" class="mt-1 block w-full px-4 py-3 bg-background border border-foreground/20 rounded-lg shadow-sm focus:ring-primary focus:border-primary text-foreground">
          </div>
          <div class="md:col-span-2">
            <label for="mensaje" class="block text-sm font-medium text-foreground/90">
              Cuéntanos sobre tu negocio y qué necesitas
            </label>
            <textarea id="mensaje" name="mensaje" rows="4" class="mt-1 block w-full px-4 py-3 bg-background border border-foreground/20 rounded-lg shadow-sm focus:ring-primary focus:border-primary text-foreground"></textarea>
          </div>
        </div>
        <div class="mt-6 text-center">
          <button type="submit" class="bg-gradient-brand text-primary-foreground font-bold py-3 px-12 rounded-lg hover:opacity-90 transition-opacity shadow-lg">
            Quiero que mi negocio respire digital ✨
          </button>
        </div>
      </form>
      <div class="mt-8 text-center text-foreground/80">
        <p class="font-semibold">O escríbenos directamente:</p>
        <p class="mt-2 text-lg">
          📧 <a href="mailto:hola@tulatidodigital.com" class="text-primary hover:text-primary/80">
            hola@tulatidodigital.com
          </a>
        </p>
        <p class="mt-2 text-lg">
          📢 Instagram: <a href="https://instagram.com/tulatidodigital" target="_blank" class="text-primary hover:text-primary/80">
            @tulatidodigital
          </a>
        </p>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const form = document.getElementById('contact-form');
    
    form?.addEventListener('submit', function(e) {
      e.preventDefault();
      
      // Aquí puedes agregar la lógica para enviar el formulario
      // Por ejemplo, usar Formspree, Netlify Forms, o tu propia API
      
      const formData = new FormData(form as HTMLFormElement);
      const data = Object.fromEntries(formData);
      
      // Mostrar mensaje de confirmación
      showSuccessMessage();
      
      // Reset form
      (form as HTMLFormElement).reset();
    });
    
    function showSuccessMessage() {
      const button = form?.querySelector('button[type="submit"]') as HTMLButtonElement;
      if (button) {
        const originalText = button.innerHTML;
        button.innerHTML = '¡Mensaje enviado! 🎉';
        button.disabled = true;
        button.classList.add('bg-green-600');
        button.classList.remove('bg-gradient-teal');
        
        setTimeout(() => {
          button.innerHTML = originalText;
          button.disabled = false;
          button.classList.remove('bg-green-600');
          button.classList.add('bg-gradient-teal');
        }, 3000);
      }
    }
  });
</script> 